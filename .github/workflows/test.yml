name: 打包react项目

on: push

jobs:
  npm-build:
    name: npm-build工作
    runs-on: ubuntu-latest
    steps:
      -name" 读取仓库内容
      - uses: actions/checkout@v6
      - run: pwd
      - run: npm --version

      - name: 安装依赖
      - run: npm install

      - name: 打包项目
      - run: npm run build